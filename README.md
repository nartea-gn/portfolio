# Nartea Portfolio (React + TS + Vite)

- Router: HashRouter (no 404 hacks on GitHub Pages)
- Styles: Tailwind (layout/utilities) + CSS Modules (complex/animations)
- Deploy: GitHub Actions → GitHub Pages (on push to `main`)

## Local

```bash
npm install
npm run dev
```
